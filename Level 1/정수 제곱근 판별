# 내 풀이
import math
def solution(n):
    if math.sqrt(n) % 1 == 0:
        return (int(math.sqrt(n)) + 1) ** 2
    else:
        return -1
        
# 다른 사람의 풀이
def solution(n):
    sqrt = n ** (1/2)
    if sqrt % 1 == 0:
        return (sqrt + 1) ** 2
    else:
        return -1
